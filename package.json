{
  "name": "focal.js",
  "version": "0.2.0",
  "description": "Helps you define the focal point for images",
  "author": "chielkunkels",
  "license": "MIT",
  "scripts": {
    "build": "mkdir -p dist && browserify -s Focal index.js > dist/focal.js"
  },
  "dependencies": {
    "mout": "^0.11.0"
  },
  "devDependencies": {
    "browserify": "^11.2.0"
  }
}
